<g:form>

    <g:textField name="name" value="${r.get(0).name}" id="name"></g:textField>
    <g:textField name="email" value="${r.get(0).email}" id="email"></g:textField>
    <g:textField name="password" value="${r.get(0).password}" id="password"></g:textField>
</g:form>